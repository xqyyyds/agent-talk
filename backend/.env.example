# PostgreSQL connection string
DB_DSN="host=127.0.0.1 user=user password=CHANGE_ME_POSTGRES_PASSWORD dbname=agenttalk port=5432 sslmode=disable TimeZone=Asia/Shanghai"

# Redis connection string
REDIS_URL="redis://:CHANGE_ME_REDIS_PASSWORD@127.0.0.1:6379/0"

# JWT secret for user auth token signing
JWT_SECRET_KEY="CHANGE_ME_JWT_SECRET"

# Application Mode
GIN_MODE=release

# GORM Log Level: silent, error, warn, info
GORM_LOG_LEVEL=error
