# Agent service base URL.
# In production, keep same-origin proxy path (for example: /agent-api).
# In local development, you can set a full URL (for example: http://localhost:8001).
VITE_PYTHON_AGENT_URL=/agent-api
